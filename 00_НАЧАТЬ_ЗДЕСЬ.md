# Codex Pulse

## Что это

Нативное macOS-приложение для контроля AI-бюджета: лимиты Codex/Claude, локальная аналитика токенов и стоимости, модели и проекты, прогноз темпа, адаптивный виджет и маршрут новой задачи.

## Статус

- Статус: публичная preview-версия 0.5.2 (31) опубликована на GitHub, собрана для Intel и Apple Silicon и установлена канонически в `~/Applications/Codex Pulse.app`; 35 Swift-тестов и popup/parser-регрессия проходят, подпись и release-checksums валидны.
- Hotfix 0.5.2: `LSMultipleInstancesProhibited` запрещает второй экземпляр через Launch Services; regression-check закреплён в `Scripts/test.sh`. Старые локальные build 29 из рабочей папки и `/private/tmp` перенесены в Корзину, автозапуск заново включён из версии в `~/Applications`.
- Результат диагностики 2026-08-12: legacy-CodexBar не видит профиль Chrome с Claude. Поэтому основной путь — собственное расширение: cookie и чаты остаются в Chrome, в Pulse уходят только проценты, время сброса и метка тарифа.
- Точка продолжения: live Claude Web сопряжён и проверен на Claude Max 5x, включая отдельную Fable-строку. До публичной beta остались onboarding, Developer ID, notarization и update channel.
- Последняя проверка: 2026-08-12.
- Проверено: universal arm64+x86_64 `.app`, строгая проверка ad-hoc подписи, живой Codex App Server, отдельные окна Codex/Codex Spark, тариф ChatGPT Pro, локальная аналитика 7/30/90 дней, интерактивная активность по дням, история расхода, Task Router, Simple/Pro, RU/EN, light/dark и Mini/Compact/Focus/Adaptive widget.
- UX-проход 0.3: меню стало информативнее, отключённый Claude больше не выглядит подключённым, компактный widget не дублирует иконку, кнопка разворачивания работает, а Focus остаётся в видимой области экрана.
- Исправление 0.3.1: системный menu-bar поповер получил устойчивую высоту и больше не схлопывает контент; Adaptive widget меняет компоновку, заполняет увеличенное окно полезными данными и запоминает осмысленный пользовательский размер.
- Исправление 0.3.2: непонятные «Календарь» и «Чеки задач» заменены рабочими сценариями «По дням» и «История расхода»; день раскрывает токены, стоимость, модель и проекты, переход сохраняет фильтр даты, пустые категории не создают немую пустоту, а 90-дневный экран больше не зависает.
- Исправление 0.3.3: 7/30/90 дней переключаются из одного фонового 90-дневного снимка локально и больше не запускают повторный cost scan; вкладки использования остаются интерактивными во время обновления провайдеров.
- Релиз 0.4.0: Task Router стал гибридным — мгновенный локальный маршрут, явный эфемерный AI-анализ через текущую Codex-сессию, уверенность, тип задачи, защитные пороги и план этапов для масштабной работы.
- Исправление 0.4.1: интерфейс явно различает «лимит обновится» и «при текущем темпе закончится»; прогноз виден рядом с квотой, а во всех размерах floating widget есть доступная кнопка ручного обновления. Минимальный Focus больше не обрезает предупреждение.
- Исправление 0.4.2: Compact widget снова явно показывает время обновления недельного лимита над отдельным прогнозом исчерпания; menu-bar popover получил текстовую кнопку «Обновить» с busy-состоянием и ⌘R.
- Исправление 0.4.3: Compact получил безопасную высоту 138 pt и уплотнённые отступы; слишком плоский сохранённый или ручной размер больше не режет нижнюю строку, а переключается в Mini.
- Исправление 0.4.4: широкий сохранённый frame 490 × 108 pt снова использует информативный Compact, а не пустой Mini; вертикальный ритм ужат до полного размещения четырёх строк. Mini включается только ниже 100 pt.
- Исправление 0.4.5: стандартный Compact стал уже — 320 × 138 pt; раскрытые Focus и Expanded прокручивают аналитический контент под закреплённым заголовком, поэтому нижние метрики, график, модель и проект доступны без растягивания окна.
- Исправление 0.4.8: системные macOS traffic lights полностью скрыты у floating panel; собственные refresh, expand/collapse и close постоянно видны справа внутри widget и не дублируются вторым набором оконных кнопок.
- Исправление 0.4.9: общий блок темпа/статуса в Focus и Expanded растягивается на всю ширину колонки и выровнен с квотами, метриками и графиком.
- Исправление 0.4.10: Compact по умолчанию уменьшен до 260 × 138 pt; старые стандартные frame 320/360 pt мигрируют один раз, а реальные ручные Adaptive-размеры сохраняются.
- Версия 0.5.0: добавлено явное подключение Claude Web через Chrome, источники Web/OAuth/Auto, отдельные окна сессии, недели и моделей, usage credits, понятные permission/error состояния и отображение Claude в dashboard, menu bar и расширенном widget. Pulse не хранит browser credentials.
- Исправление 0.5.0 build 21: ключ локализации `pairing` больше не перезаписывает весь popup-блок, поэтому поле кода и кнопка снова видны. Код сопряжения теперь создаётся автоматически при выбранном Pulse Connector; добавлен отдельный Node regression check.
- Исправление 0.5.0 build 22: live-smoke test прошёл на Claude Max 5x; dashboard, settings, menu bar и widget показывают «осталось», «использовано» и тип окна; известное время сброса приоритетнее пустого, а обезличенный снимок лимитов переживает перезапуск до 24 часов. Focus widget всегда даёт доступ ко всем окнам через прокрутку.
- Исправление 0.5.0 build 23: Pulse Connector 0.1.3 читает модельные недельные лимиты из актуального массива Claude `limits`, поэтому Fable отображается отдельным окном во всех поверхностях. Старый `seven_day_fable` сохранён как fallback, а совпадающие значения не дублируются.
- Исправление 0.5.0 build 24: floating widget больше не показывает общий прогноз после списка Claude. Квоты Codex и Claude собраны в отдельные группы, и у каждой сразу под её окнами расположен собственный темп, прогноз исчерпания и безопасный расход на сегодня. Compact явно называет сервис, к которому относится единственный приоритетный прогноз.
- Исправление 0.5.0 build 25: экран использования разделён на «Лимиты и темп» и «Токены и стоимость». Pulse локально сохраняет отдельную 90-дневную историю текущей сессии Claude, общей недели, Fable и окон Codex, строит процентные графики и события сбросов; токены, проекты и стоимость показываются только для источников, которые действительно их предоставляют.
- QA build 25: округление `resets in` у провайдеров больше не создаёт ложный «новый период» каждые несколько минут. История автоматически очищает такой старый шум, но сохраняет реальные изменения процента, настоящие смены окна и 30-минутные контрольные точки.
- Исправление 0.5.0 build 26: menu bar Overview больше не смешивает токеновый график Codex с квотами Claude. Codex и Claude получили равноправные блоки «окна лимита → собственный темп → динамика остатка»; у Claude явно отображается собственный нормальный/опасный статус. Вторая вкладка переименована в «Токены Codex» и объясняет, почему Claude Web там не представлен.
- Исправление 0.5.0 build 27: стандартный режим «Процент» в строке меню больше не показывает анонимный минимум. Он выводит две постоянные монохромные пары «значок Codex + остаток» и «значок Claude + остаток»; недоступный сервис честно показывает `—`. Ширина больше не скачет при позднем ответе Claude Connector; VoiceOver и tooltip явно называют оба сервиса.
- Исправление 0.5.0 build 28: в Compact/Mini floating widget верхние индикаторы Codex и Claude стали кликабельным переключателем. По умолчанию выбран Codex; сброс, темп и прогноз всегда относятся к явно выбранному сервису. Активный сервис показан подложкой и именем; при отключении Claude виджет безопасно возвращается к Codex. Focus/Expanded по-прежнему показывают оба провайдера целиком.
- Проверка 2026-08-11: установлен рекомендованный Apple Command Line Tools 27.0 beta 5; `Scripts/test.sh` запускает реальный Swift Testing suite в CLT-only окружении, 20 тестов в 4 наборах проходят.
- Ближайший шаг: собирать обратную связь по public preview, улучшить onboarding и подготовить подписанный/notarized канал обновлений после появления Apple Developer ID.

## Источники истины

- Код и сборка: текущая папка, `Sources/CodexPulse/`, `Package.swift`, `Scripts/build-app.sh`.
- Codex-интеграция: официальная документация Codex App Server — `account/rateLimits/read` и `account/usage/read`.
- Claude-интеграция: provider adapter с безопасным локальным fallback; живую авторизацию проверять на подключённом аккаунте.
- Визуальный источник: `Resources/design-reference.png`.
- Не считать актуальными без проверки: концептуальные изображения и данные-заглушки в старых макетах.

## Навигация

- `Sources/CodexPulse` — нативное приложение.
- `Resources` — визуальный источник и иконка.
- `Scripts` — сборка `.app`.
- `docs` — продукт, архитектура, приватность и roadmap.

## Ограничения

- Не публиковать в GitHub и не создавать релиз без отдельного подтверждения.
- Не хранить ключи, cookies, токены, email аккаунтов и тексты задач. AI-маршрут может обработать только явно введённый текст после нажатия пользователя; Pulse его не сохраняет.
- Не заявлять поддержку Claude как проверенную на конкретном аккаунте, пока он не подключён и не пройдён живой smoke test.
